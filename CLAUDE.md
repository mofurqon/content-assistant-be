# AI Content Assistant — CLAUDE.md

## Project Overview
An AI agent that helps users generate high-quality articles from a given topic.
The agent uses a RAG pipeline backed by an internal knowledge base (PDF), a self-evaluation
and improvement loop, web research enrichment, and image prompt generation.

This is a case study prototype — not production-ready.

---

## Stack
- **Language**: Python 3.11+
- **UI**: Streamlit (streaming supported)
- **LLM + Embeddings**: Google Gemini (Flash for LLM, embedding-001 for embeddings)
- **Orchestration**: LangChain
- **Vector DB**: Chroma (local, persistent)
- **Web Research**: Firecrawl (or mocked)

---

## Folder Structure
```
ai-content-assistant/
├── app.py                  # Streamlit entry point
├── config/
│   ├── settings.py         # Env loading + require_env (was agent/env.py)
│   └── constants.py        # All non-prompt tuning constants (paths, TOP_K, thresholds, CRITERIA, chunk/batch, firecrawl)
├── core/
│   └── llm.py              # Cached get_llm / get_embeddings factory (was agent/llm.py)
├── agent/
│   ├── pipeline.py         # Main agent orchestration (calls all steps in order)
│   ├── ideas.py            # Step 1: Generate content ideas
│   ├── retriever.py        # Step 2: RAG retrieval from Chroma
│   ├── generator.py        # Step 3: Draft generation with KB context
│   ├── evaluator.py        # Step 4: Self-evaluation (score + reasoning)
│   ├── improver.py         # Step 5: Improvement loop (max 2 iterations)
│   ├── researcher.py       # Step 6: Web research enrichment
│   └── finalizer.py        # Step 7: Final content + image prompt
├── ingest/
│   └── ingest.py           # One-time PDF ingestion → Chroma
├── kb/
│   └── software-testing-guide-book.pdf
├── chroma_db/              # Auto-generated by Chroma on first ingest
├── .env                    # GEMINI_API_KEY (never commit)
├── .env.example            # Safe to commit
├── requirements.txt
├── README.md
└── CLAUDE.md               # This file
```

---

## Agent Pipeline (ordered steps)
1. **Idea Generation** — LLM generates 3-5 content ideas from user input
2. **User Selects Idea** — Streamlit UI lets user pick one idea
3. **RAG Retrieval** — Selected idea is embedded, top-K chunks fetched from Chroma
4. **Draft Generation** — LLM writes structured draft using KB chunks as context
5. **Self-Evaluation** — LLM scores draft on 6 criteria (1–5 scale each):
   - Clarity, Relevance, Completeness, Accuracy, Actionability, KB Alignment
6. **Improvement Loop** — If average score < 4.0, improve and re-evaluate. Max 2 iterations.
7. **Web Research** — Generate search queries, fetch and summarize external sources
8. **Final Output** — Combine improved draft + research → final article + image prompt

---

## Key Design Decisions
- **Stopping rule**: Max 2 improvement iterations OR average eval score ≥ 4.0
- **Idea selection**: Human-in-the-loop via Streamlit UI (satisfies case study section 6c)
- **KB scope**: Fixed pre-ingested PDF, no dynamic upload required
- **Streaming**: User-facing prose (draft + final article) streams token-by-token to Streamlit via `st.write_stream` (`stream_draft` in generator.py, `stream_article` in finalizer.py). Internal structured steps (evaluation, improvement, research, image prompt) run blocking under spinners/status.
- **Embeddings**: Same Gemini provider for both LLM and embeddings (single API key)
- **Client reuse**: LLM/embedding clients are built by a cached factory in `core/llm.py` (`get_llm(temperature)` / `get_embeddings()`, `@lru_cache`) — one instance per temperature is reused process-wide instead of rebuilt on every call
- **Config separation**: cross-cutting code lives outside `agent/` — env loading in `config/settings.py`, all non-prompt tuning knobs in `config/constants.py`, LLM clients in `core/llm.py`. `agent/` holds only step logic + pipeline; prompt templates stay inline with their functions

---

## Environment Variables
```
GEMINI_API_KEY=your_key_here
GEMINI_MODEL=gemini-2.0-flash             # LLM model used for all generation steps
GEMINI_EMBED_MODEL=embedding-001          # embedding model (prefixed with models/ at use)
FIRECRAWL_API_KEY=your_key_here           # optional, can be mocked
```
All required vars are validated at use via `require_env()` in `config/settings.py`.

---

## Commands
- `python -m ingest.ingest` — Run once to ingest the KB PDF into Chroma (run from project root)
- `streamlit run app.py` — Start the Streamlit UI

---

## Phase Gates
Work is done phase by phase. Do not move to the next phase until the current one is verified.

### Phase 1 — Project Setup ✅
- [x] Folder structure
- [x] CLAUDE.md
- [x] .claude/commands
- [x] requirements.txt
- [x] .env.example

### Phase 2 — Ingest Pipeline ✅
- [x] ingest.py (PDF → chunk → embed → Chroma)
- [x] Verify chunks are stored correctly

### Phase 3 — Agent Steps ✅
- [x] ideas.py
- [x] retriever.py
- [x] generator.py
- [x] evaluator.py
- [x] improver.py
- [x] researcher.py
- [x] finalizer.py
- [x] pipeline.py

### Phase 4 — Streamlit UI ✅
- [x] app.py with streaming (st.write_stream for draft + final article)
- [x] Idea selection UI
- [x] Step-by-step output display

### Phase 5 — Polish & Docs
- [ ] README.md
- [ ] Sample input/output
- [ ] Trade-offs section in docs

---

## Rules for Claude Code
- Always work within the current phase — do not skip ahead
- Each agent step lives in its own file — keep them small and focused
- No FastAPI, no REST endpoints — Streamlit calls Python functions directly
- Never hardcode API keys — always use .env
- Keep LLM prompts in the same file as the function that uses them
- After each phase, update the phase gate checkboxes above
